# API Configuration
# Base URL for backend API (used by axiosClient)
NEXT_PUBLIC_BASE_URL=http://localhost:5001/api

# App Metadata (used by logger and diagnostics)
NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_APP_NAME=lne-frontend-admin
NEXT_PUBLIC_APP_VERSION=0.1.0

# Next.js Runtime (NODE_ENV is set by the runtime; keep here only for reference)
# NODE_ENV=development

# Optional: Analytics and Monitoring
# NEXT_PUBLIC_GA_ID=your-google-analytics-id
# NEXT_PUBLIC_SENTRY_DSN=your-sentry-dsn

# Optional: External Services
# NEXT_PUBLIC_API_KEY=your-api-key
# DATABASE_URL=your-database-url

# Optional: Feature Flags
# NEXT_PUBLIC_ENABLE_FEATURE_X=true
# NEXT_PUBLIC_ENABLE_FEATURE_Y=false